<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/image_picker_colorPrimary"
    android:elevation="4dp"
    tools:targetApi="lollipop">

    <ImageButton
        android:id="@+id/btn_image_picker_title_back"
        style="@style/ThemeOverlay.AppCompat.Dark"
        android:layout_width="50dp"
        android:layout_height="match_parent"
        android:background="?attr/selectableItemBackgroundBorderless"
        android:src="@drawable/image_picker_ic_arrow_back_white_24dp" />

    <TextView
        android:id="@+id/txt_image_picker_title_title"
        style="@style/TextAppearance.AppCompat.Widget.ActionBar.Title"
        android:layout_width="0dp"
        android:layout_height="match_parent"
        android:layout_weight="1"
        android:background="?attr/selectableItemBackgroundBorderless"
        android:ellipsize="middle"
        android:gravity="center_vertical"
        android:singleLine="true"
        android:textColor="@color/image_picker_text_color_normal" />

    <TextView
        android:id="@+id/btn_image_picker_title_detail"
        style="@style/ThemeOverlay.AppCompat.Dark"
        android:layout_width="wrap_content"
        android:layout_height="30dp"
        android:layout_gravity="center_vertical"
        android:layout_marginLeft="10dp"
        android:layout_marginRight="10dp"
        android:background="@drawable/image_picker_selector_btn_confirm"
        android:gravity="center"
        android:paddingLeft="10dp"
        android:paddingRight="10dp"
        android:text="@string/image_picker_send"
        android:textColor="@color/image_picker_send_text_color"
        android:textSize="12sp" />

</LinearLayout>
